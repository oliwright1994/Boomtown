const styles = theme => ({
  return: {
    fontSize: 11,
    color: theme.palette.primary.main,
    borderColor: theme.palette.primary.main
  },
  borrow: {
    fontSize: 11
  },
  error: {
    fontSize: 11,
    color: "red"
  }
});

export default styles;
